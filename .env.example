# Backend server port
PORT=3000

# Path to SQLite database file.
# Can be relative to the 'server' directory or an absolute path.
# Default: database.sqlite
DATABASE_PATH=database.sqlite

# Path to uploads directory for audio and images.
# Can be relative to the 'server' directory or an absolute path.
# Default: uploads
UPLOAD_DIR=uploads
